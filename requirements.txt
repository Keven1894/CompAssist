# Core dependencies
python-dotenv>=1.0.0
psutil>=5.9.0
requests>=2.31.0
pyyaml>=6.0
colorama>=0.4.6

# Windows-specific (optional, installs only on Windows)
pywin32>=306; sys_platform == 'win32'
wmi>=1.5.1; sys_platform == 'win32'

# Docker and deployment
docker>=6.1.0

# Development
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.7.0
flake8>=6.1.0


